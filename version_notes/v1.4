# v1.4 Release Notes

## Features
- new `ctx.FormBool()` and `ctx.FormFloat()` functions for retrieving form values as boolean and float types.

- new .env file support for loading environment variables from a file.

- Sqlite3 support added.

- Update Fakedata
